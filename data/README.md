# Data Directory

- `raw/`: script-downloaded raw files, not tracked by default
- `processed/`: derived caches and toy data, not tracked by default except for
  small examples
- `external/`: references and small external annotations when legally
  redistributable
- `manifests/`: deterministic manifest and audit tables
